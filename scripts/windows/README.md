# Windows Helpers

Windows users should build the ISO with the GitHub Actions workflow first:

```text
.github/workflows/build-nexos-iso.yml
```

Read:

```text
docs/windows-no-linux-build.md
```

After downloading the ISO artifact from GitHub Actions, test it in Oracle VirtualBox on Windows.
